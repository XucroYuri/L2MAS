# L2MAS runtime dependencies for the current MVP prototype.
#
# A2A and MCP compatibility layers are provided locally in ./a2a and ./mcp
# until the upstream protocol packages and versions stabilize for this repo.
# Live2D Cubism SDK, Textoon, ComfyUI, Diffusers, and media-model workers are
# optional provider integrations and are not installed by the default MVP path.

python-dotenv==1.0.1
httpx==0.27.0
aiohttp==3.9.5
requests==2.32.3
prometheus-client==0.20.0
structlog==24.2.0
typing-extensions==4.11.0
tqdm==4.67.3
